{
    "name": "aov",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "cd client && npm install && npm run build && cd server && npm install && npm start",
      "start": "cd server && npm install && npm start"
    }
  }
  